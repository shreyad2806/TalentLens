"""
TalentLens metadata normalization layer.

Each normalizer canonicalizes a single resume field (role, degree, skill,
location, experience). ``MetadataNormalizer`` orchestrates them on a full
``ResumeDocument`` so downstream components always see consistent values.
"""

"""Deprecated metadata normalization layer."""

__all__ = []
